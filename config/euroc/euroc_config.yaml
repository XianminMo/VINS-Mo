%YAML:1.0

#common parameters
imu_topic: "/imu0"
image_topic: "/cam0/image_raw"

# output_path: "/home/linux/mxm/output/experiments_backend/V203_test/1216/o-d/test/k_0.5/0"
# output_path: "/home/linux/mxm/output/experiments_backend/MH04_test/1216/new/o-d/k_15/0"
output_path: "/home/linux/mxm/output_VINS-Mo/experiments_backend/V201_test/1217/o-d/k_10/5"

# depth_model_path: "/home/linux/mxm/proj/VINS-Mo/src/VINS-Mo/vins_estimator/models/Midas-V2.onnx" # path to depth estimation model
depth_model_path: "/home/linux/mxm/proj/VINS-Mo/src/VINS-Mo/vins_estimator/models/depth_anything_v2_vits.onnx" # path to depth estimation model

use_fast_init: 0      # 0: normal initialization; 1: use fast initialization with depth estimation

# fast init parameters (optional; defaults used if omitted)
fast_init.min_features: 50                 # 最小特征数阈值（初始化前检测）
fast_init.min_acc_var: 0.15               # IMU加速度方差下限，用于激励检测（无单位）
fast_init.ransac.min_measurements: 4      # RANSAC最小观测对数量
fast_init.ransac.max_iterations: 500      # RANSAC最大迭代次数
fast_init.ransac.residual_thresh_px: 0.01 # RANSAC残差阈值（像素）
fast_init.ransac.min_inliers: 20          # RANSAC最少内点数
fast_init.ransac.early_exit_inlier_ratio: 0.7 # 早停：内点比例超过该值即提前退出
fast_init.svd.min_sigma: 1.0e-8           # SVD最小奇异值阈值（数值稳定性）
fast_init.cond.threshold: 3.0e5          # 线性系统条件数上限（数值稳定性）
fast_init.depth.inv_min: 1.0e-6           # 归一化逆深度最小值（有效性检查）
fast_init.depth.inv_max: 10.0             # 归一化逆深度最大值（有效性检查）
fast_init.depth.z_min: 0.1                # 有效景深下限（米）
fast_init.depth.z_max: 50.0               # 有效景深上限（米）
fast_init.depth.min_valid_features: 10    # 有效深度特征的最少数量
fast_init.irls.iters: 3                   # IRLS迭代次数
fast_init.irls.huber_delta: 1.5e-2        # IRLS Huber阈值（像面误差尺度）
fast_init.reg.lambda_a: 1.0e-2            # 正则：尺度因子a的权重
fast_init.reg.lambda_b: 1.0e-2            # 正则：深度偏移b的权重
fast_init.reg.lambda_v: 1.0e-3            # 正则：初始速度v的权重

# ========================================================================
# Backend Depth Constraint Parameters (后端深度约束参数)
# ========================================================================
# 架构说明: Hierarchical Decoupling Depth Fusion (层次解耦深度融合)
#   - 前端: SolveLinearAlignment() 每帧计算尺度s和偏移t
#   - 后端: Ceres优化每帧独立的para_a_global[i] (初始值=1.0)
#   - 约束: RandomWalkFactor 保证相邻帧的para_a平滑变化
#   - 损失: 固定使用 HuberLoss(1.0), 不再使用动态权重
#   - 优势: 消除全局参数漂移,提高鲁棒性
#
# 是否在后端优化中启用深度约束因子 (0: 关闭, 1: 开启)
depth_constraint.estimate_scale_shift: 1

# 深度尺度参数 (a) 的随机游走过程噪声 (Random Walk Process Noise for Scale)
# 控制 para_depth_scale[i] 在相邻帧之间允许的最大变化量
# 值越大: 参数变化越自由,能快速适应尺度跳变,但可能震荡
# 值越小: 参数变化越保守,更加平滑,但响应较慢
# 推荐范围: 1e-4 到 1e-3
depth_constraint.random_walk_a: 5.0e-4

# 深度偏移参数 (b) 的随机游走过程噪声 (Random Walk Process Noise for Shift)
# 控制 para_depth_shift[i] 在相邻帧之间允许的最大变化量
# 设计理念: "Lazy Buffer" - 使用极小噪声让b参数保持惰性,仅在必要时变化
# 值越大: b 越活跃,可能引入不必要的自由度
# 值越小: b 越惰性,保持接近零,提高系统稳定性
# 推荐范围: 1e-6 到 1e-4 (应远小于 random_walk_a)
depth_constraint.random_walk_b: 5.0e-6

# ========================================================================
# 深度权重调参系数 K (Depth Weight Tuning Parameter)
# ========================================================================
# K 用于深度因子权重公式: W = 1.0 / (K × σ + ε)²
# 作用: 平衡视觉重投影误差和深度微调误差的贡献
#
# K 越大 → 权重越小 → 深度约束越弱 (更信任视觉)
# K 越小 → 权重越大 → 深度约束越强 (更信任深度)
#
# 调参历史:
#   - K=2.0 (配置值, 但实际运行 K=1.0 由于Bug):
#       平均 Balance Ratio = 2.11 (接近目标,但波动大)
#       分布: 35.8%太弱(<0.2), 55.6%合适, 8.6%太强(>5.0)
#       极端值: 0.0001 ~ 34.48 (不稳定)
#       结论: K=1.0效果尚可,需微调以减少"太弱"比例
#   - K=1.0 (当前推荐): 基于实际测试结果
#       说明: 修复Bug后,从实际生效的K=1.0开始调参
#
# 调参建议:
#   - 如果 Balance Ratio < 0.2: K 太大,深度约束太弱,减小K
#   - 如果 Balance Ratio > 5.0: K 太小,深度约束太强,增大K
#   - 目标: Balance Ratio ≈ 1.0 (深度和视觉成本相当)
#   - 追踪: 减少 <0.2 的比例 (当前35.8% → 目标<20%)
#
# 推荐范围: 0.5 ~ 2.0
depth_constraint.weight_k: 10

# ========================================================================
# 已废弃参数 (Deprecated Parameters) - 保留用于参考
# ========================================================================
# 以下参数在新架构中已不再使用,保留注释仅供历史参考:
#
# 1. 全局初值参数 (已废弃)
# depth_constraint.initial_scale_a: 0.25      # ❌ 每帧para_a初始化为1.0
# depth_constraint.initial_shift_b: 0.35      # ❌ 偏移由前端对齐处理
# depth_constraint.random_walk_b: 1.0e-4      # ❌ 不再优化shift参数b
#
# 2. 权重模式和自适应参数 (已废弃)
# depth_constraint.weight_mode: 0                    # ❌ 固定使用HuberLoss(1.0)
# depth_constraint.weight: 0.25                      # ❌ 不再使用可配置权重
# depth_constraint.huber_threshold: 0.5              # ❌ 固定使用1.0阈值
# depth_constraint.warmup_frames: 30                 # ❌ 不再使用预热机制
# depth_constraint.adaptive.weight_static: 5.0       # ❌ 不再使用自适应权重
# depth_constraint.adaptive.weight_dynamic: 1.0      # ❌ 不再使用自适应权重
# depth_constraint.adaptive.physical_error_threshold: 0.25   # ❌
# depth_constraint.adaptive.instability_threshold_low: 0.3   # ❌
# depth_constraint.adaptive.instability_threshold_high: 1.5  # ❌
# depth_constraint.adaptive.acc_disturbance_weight: 0.5      # ❌
#
# 废弃时间: 2025-12-14
# 废弃原因:
#   - 新架构使用每帧独立优化para_a_global[i],不需要全局初值
#   - shift参数b由前端SolveLinearAlignment处理,后端只优化scale参数a
#   - 后端统一使用HuberLoss(1.0),简化设计,不再需要动态权重调整
# ========================================================================

#camera calibration 
model_type: PINHOLE
camera_name: camera
image_width: 752
image_height: 480
distortion_parameters:
   k1: -2.917e-01
   k2: 8.228e-02
   p1: 5.333e-05
   p2: -1.578e-04
projection_parameters:
   fx: 4.616e+02
   fy: 4.603e+02
   cx: 3.630e+02
   cy: 2.481e+02

# Extrinsic parameter between IMU and Camera.
estimate_extrinsic: 0   # 0  Have an accurate extrinsic parameters. We will trust the following imu^R_cam, imu^T_cam, don't change it.
                        # 1  Have an initial guess about extrinsic parameters. We will optimize around your initial guess.
                        # 2  Don't know anything about extrinsic parameters. You don't need to give R,T. We will try to calibrate it. Do some rotation movement at beginning.                        
#If you choose 0 or 1, you should write down the following matrix.
#Rotation from camera frame to imu frame, imu^R_cam
extrinsicRotation: !!opencv-matrix
   rows: 3
   cols: 3
   dt: d
   data: [0.0148655429818, -0.999880929698, 0.00414029679422,
           0.999557249008, 0.0149672133247, 0.025715529948, 
           -0.0257744366974, 0.00375618835797, 0.999660727178]
#Translation from camera frame to imu frame, imu^T_cam
extrinsicTranslation: !!opencv-matrix
   rows: 3
   cols: 1
   dt: d
   data: [-0.0216401454975,-0.064676986768, 0.00981073058949]

#feature traker paprameters
max_cnt: 150            # max feature number in feature tracking
min_dist: 30            # min distance between two features 
freq: 10                # frequence (Hz) of publish tracking result. At least 10Hz for good estimation. If set 0, the frequence will be same as raw image 
F_threshold: 1.0        # ransac threshold (pixel)
show_track: 1           # publish tracking image as topic
equalize: 1             # if image is too dark or light, trun on equalize to find enough features
fisheye: 0              # if using fisheye, trun on it. A circle mask will be loaded to remove edge noisy points

#optimization parameters
max_solver_time: 0.04  # max solver itration time (ms), to guarantee real time
max_num_iterations: 8   # max solver itrations, to guarantee real time
keyframe_parallax: 10.0 # keyframe selection threshold (pixel)

#imu parameters       The more accurate parameters you provide, the better performance
acc_n: 0.08          # accelerometer measurement noise standard deviation. #0.2   0.04
gyr_n: 0.004         # gyroscope measurement noise standard deviation.     #0.05  0.004
acc_w: 0.00004         # accelerometer bias random work noise standard deviation.  #0.02
gyr_w: 2.0e-6       # gyroscope bias random work noise standard deviation.     #4.0e-5
g_norm: 9.81007     # gravity magnitude

#loop closure parameters
loop_closure: 1                    # start loop closure
load_previous_pose_graph: 0        # load and reuse previous pose graph; load from 'pose_graph_save_path'
fast_relocalization: 1             # useful in real-time and large project
pose_graph_save_path: "/home/linux/mxm/output/pose_graph/" # save and load path

#unsynchronization parameters
estimate_td: 0                      # online estimate time offset between camera and imu
td: 0.0                             # initial value of time offset. unit: s. readed image clock + td = real image clock (IMU clock)

#rolling shutter parameters
rolling_shutter: 0                  # 0: global shutter camera, 1: rolling shutter camera
rolling_shutter_tr: 0               # unit: s. rolling shutter read out time per frame (from data sheet). 

#visualization parameters
save_image: 1                   # save image in pose graph for visualization prupose; you can close this function by setting 0 
visualize_imu_forward: 0        # output imu forward propogation to achieve low latency and high frequence results
visualize_camera_size: 0.4      # size of camera marker in RVIZ
